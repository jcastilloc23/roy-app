Read @STMTS_SCHEMA.md before proceeding. Use it as the canonical reference for how Roy ingests, normalizes, and stores royalty statement data.

Apply this schema context to the following task — whether that's accepting a new statement format, validating parser output, mapping fields from a new source, or debugging a parsing issue:

$ARGUMENTS
